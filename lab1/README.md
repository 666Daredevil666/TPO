# Лабораторная работа 1

## Вариант `111300`

Структура проекта: три отдельных задания, Gradle-модули `task1`, `task2`, `task3`, а также отчёт в папке `docs`.

### Задание

1. Для указанной функции провести модульное тестирование **разложения функции в степенной ряд**.  
   Функция: `cos(x)`.
2. Провести модульное тестирование указанного алгоритма.  
   Алгоритм: **пирамидальная сортировка массива (HeapSort)**.
3. Сформировать доменную модель для заданного текста и разработать тестовое покрытие.

> Форд, в изрядном замешательстве, покопался в нем. Эти миниатюрные ранцевые компьютеры обычно полностью дублировались главным компьютером корабля, с которым они были связаны по суб-эфиру. Такая система была безотказной в любых условиях, кроме всеобщего сбоя оборудования, чего быть просто не могло.

- `task1` — реализация `cos(x)` через степенной ряд и JUnit-тесты;
- `task2` — heapsort с трассировкой характерных точек и тестами эталонных последовательностей;
- `task3` — доменная модель предметной области и тестовое покрытие.

./gradlew test

./gradlew jacocoTestReport

./gradlew clean test jacocoTestReport --rerun-tasks --console=plain

./gradlew :task1:run
./gradlew :task2:run
./gradlew :task3:run

./gradlew :task2:test --tests "lab1.task2.algorithms.HeapSortTest"